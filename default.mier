#!/bin/bash
#export LD_PRELOAD="$CUDADIR/lib64/libcudart.so $CUDADIR/lib64/libcublas.so"
. ./values.sh

unset OMP_NUM_THREADS
python Launcher.py $xmin $xmax $ymin $maxiter own onlytimes sizes 256 512 1024 2048 methods all #| column -t -s ';'
